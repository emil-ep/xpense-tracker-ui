
# Hosting Locally (Self Hosted)

The application packages are published to GHCR and can be run easily by

`docker-compose up -d`

The application can be stopped by executing

`docker-compose down`


If you want to clear the local volume and storage, simply run

`docker-compose down -v`
